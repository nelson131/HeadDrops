plugins {
    id 'java'
}

group 'com.nelson131.headdrops'
version '1.0.0'

repositories {
    mavenCentral()
    maven { url = uri("https://repo.papermc.io/repository/maven-public/") }
}

dependencies {
    compileOnly("io.papermc.paper:paper-api:1.19-R0.1-SNAPSHOT")
}

test {
    useJUnitPlatform()
}